#include <stdio.h>

int main()
{
    int main = 3;
    printf("%d", main);
    return 0;
}

/*
Answer: 3
A C program can have same function name and same variable name.
However it is important to note that two definitions in C cannot be same. Therefore,
two variable definitions with same name, two functions definition with same name is not possible.
*/